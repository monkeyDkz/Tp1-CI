name: "CI"

on:
    push:
        paths-ignore: "./ci-baiscs.yml"
    workflow_dispatch: ~

jobs:
    deps:
        name: "Install Dependencies"
        runs-on: ubuntu-latest
        steps:
            - name: "Checkout"
              uses: actions/checkout@v2
            - run: ls
